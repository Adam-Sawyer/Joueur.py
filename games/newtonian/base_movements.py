


def in_range(map, ploc, range, desired):


    map.get_tile_at(x, y)

  return target


def stun(map, ploc, range=None, loc=None, player=None, waypoint=None):
  desired = Player()
  # action = pickup

  if range:
    o = in_range(map, ploc, range, desired)
    if o:
      # do action
      pass

    return o

  if loc:
    o =
